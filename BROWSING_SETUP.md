# Налаштування браузингу (БЕЗКОШТОВНО!)

## Що було додано

Додаток тепер підтримує **браузинг БЕЗКОШТОВНО** через DuckDuckGo для пошуку актуальних, релевантних посилань в інтернеті.

### Як це працює:

1. **DuckDuckGo Search** (БЕЗКОШТОВНО, без API key) шукає реальні посилання в інтернеті
2. **Tavily Search API** (опційно, якщо є API key) - преміум варіант з кращою якістю
3. **GPT-5.1** фільтрує та форматує знайдені посилання
4. Валідація URL через HEAD-запити (перевірка на 404)
5. Результати передаються в `/api/articles` для генерації статей

## Налаштування

### ✅ БЕЗКОШТОВНИЙ РЕЖИМ (працює за замовчуванням!)

**Нічого налаштовувати не потрібно!** Додаток автоматично використовує DuckDuckGo для пошуку.

Просто запустіть:
```bash
npm run dev
```

Браузинг працює одразу, без будь-яких API ключів!

### 🚀 Преміум режим (опційно)

Якщо хочете кращу якість пошуку, можете додати Tavily API Key:

1. Перейдіть на https://tavily.com/
2. Зареєструйтеся або увійдіть
3. Створіть новий API key в Dashboard
4. Додайте до `.env.local`:

```bash
# OpenAI API Key (обов'язково)
OPENAI_API_KEY=sk-...

# Tavily Search API Key (опційно, для кращої якості)
TAVILY_API_KEY=tvly-...
```

## Як працює браузинг

### Безкоштовний режим (DuckDuckGo):
- ✅ **Працює за замовчуванням** - нічого налаштовувати не потрібно!
- ✅ Реальний пошук в інтернеті
- ✅ Актуальні посилання
- ✅ Валідація URL (перевірка на 404)
- ✅ Релевантні deep-links для кожної теми
- ✅ Безкоштовно, без обмежень

### Преміум режим (Tavily, якщо є API key):
- ✅ Краща якість результатів
- ✅ Більше контексту для кожного результату
- ✅ 1,000 запитів/місяць безкоштовно
- ✅ Автоматично використовується, якщо API key налаштовано

## Приклад роботи

1. Користувач обирає тему: "TikTok to Spotify funnel"
2. `/api/find-links` викликає Tavily з запитом: "TikTok to Spotify funnel music promotion guide report"
3. Tavily повертає 10 релевантних посилань
4. GPT-5.1 фільтрує та обирає 2-4 найкращі
5. URL валідуються через HEAD-запити
6. Результат передається в `/api/articles`

## Вартість

### DuckDuckGo (за замовчуванням):
- ✅ **Повністю безкоштовно**
- ✅ Без обмежень на кількість запитів
- ✅ Не потребує API key
- ✅ Працює одразу після запуску

### Tavily (опційно):
- 1,000 запитів на місяць безкоштовно
- Потім $0.01 за запит
- Для середнього використання (100-200 статей на місяць) безкоштовного плану достатньо

## Який режим використовується?

Додаток автоматично вибирає найкращий доступний провайдер:

1. **Якщо є `TAVILY_API_KEY`** → використовується Tavily (преміум)
2. **Якщо немає `TAVILY_API_KEY`** → використовується DuckDuckGo (безкоштовно)

Обидва режими працюють однаково добре для пошуку релевантних посилань!

