# Помилки та уроки — TypeReach

<!-- Додавай нові помилки в форматі: -->
<!-- ## [Дата] Опис помилки -->
<!-- **Причина:** ... -->
<!-- **Рішення:** ... -->
<!-- **Урок:** ... -->

## [2026-07-16] "net-glitch": бренд знищувався в кожній статті батчу (0 згадок у 9/9)
**Причина:** ланцюг із трьох ланок. (1) Automation pipeline клав `anchorUrl` у `brief.clientSite`, а articles route витягує звідти ДОМЕН як brandName → модель писала бренд як "PromoSoundGroup.net". (2) `humanizeSectionText` мав параметр `frozenPhrases`, який call sites передавали, але реалізація його ІГНОРУВАЛА (deprecated `_frozenPhrases`) — захист фраз мовчки випав при рефакторингу, і домен ішов у Undetectable голим, де його жувало по крапці: лишався сирітський "net" зі зʼїденим початком речення. (3) Поле `brief` ("Mention PromoSoundGroup 2-3 times") API мовчки викидало — той самий клас, що й проігнорований `language`.
**Рішення:** параметр `brand` (тільки plain name, URL/домен → 400) → clientSite; frozen phrases реалізовані в `createPlaceholderProtection` (бренд+анкор токенізуються перед гуманізатором); `brief` приймається і додається до topic brief.
**Урок:** deprecated-параметр, який call sites ще передають — це бомба: виглядає як працюючий контракт, а реалізація його викинула. І бренд у промпт має йти як імʼя, ніколи як домен.

## [2026-07-16] "Деплой зламав генерацію" — а це була квота OpenAI
**Причина:** одразу після деплою automation-реворку 5/5 джоб падали з `generation_failed` за ~15с. Виглядало як регресія деплою (до — працює, після — 100% фейл). Реальна причина: `insufficient_quota` на акаунті OpenAI — остання успішна джоба допила залишок квоти, збіг у часі з деплоєм був випадковим. Агрегатна помилка articles route ("JSON parse errors, missing articleBlocks, or model truncation") ховала справжню причину в консолі.
**Рішення:** перший per-topic error тепер пробивається у відповідь (`Underlying error: 429 You exceeded your current quota...`). Діагноз підтверджено трьома способами: локальне відтворення з тою самою сигнатурою, мінімальний прямий запит до OpenAI, незмінність model-шляху в diff.
**Урок:** швидкий (секунди замість хвилин) і 100%-й фейл генерації — це майже завжди відбиття на зовнішньому API (квота/ключ/біллінг), а не модель і не свіжий код. Перш ніж відкочувати деплой — мінімальний прямий запит до провайдера. І ніколи не повертати вгадану причину помилки, коли справжня є в exception.

## [2026-07-15] Automation API: три системні дефекти, знайдені зовнішнім black-box probing
**Причина:** (1) `202 {"status":"queued"}` брехав — слот перевірявся тільки в `after()`, друга джоба тихо вмирала з `concurrency_limit`; (2) `language` приймався, але ігнорувався — `pipeline.ts` хардкодив `language: "English"`, іспанський запит гарантовано давав англійську статтю з 200 OK; (3) усі automation-джоби ділили один rate-limit бакет `generate` 10/год через `x-forwarded-for: "automation"` — батч на 10-12 статей впирався в ліміт із загадковими падіннями.
**Рішення:** справжня FIFO-черга в KV з опортуністичним дренажем (кожен POST/GET-poll — тригер), атомарні слоти SET NX + one-shot started-guard; language валідується проти `config/languages.ts` і прокидається в brief; внутрішні виклики обходять rate limiter через in-process токен (`lib/automation/internal.ts`).
**Урок:** статус у відповіді — це контракт: якщо кажеш "queued", джоба мусить колись виконатись. Параметри, що приймаються без валідації, гірші за відхилені — мовчазна деградація виявляється лише коли людина читає драфт. І get-then-set на Redis — завжди race: тільки SET NX.
